# Workbench API — installed on top of the repo's requirements.txt.
#   venv/bin/pip install -r workbench/server/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pymupdf>=1.24           # rasterise deck slides (pptx -> pdf via system `soffice`)
